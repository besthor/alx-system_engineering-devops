#!/usr/bin/env bash
# Bash script that uses ssh to connect to your server 
# using the private key ~/.ssh/school with the user ubuntu.
# Allows us to connect to my server using the private key with user ubuntu

ssh-copy-id -i ~/.ssh/school.pub ubuntu@54.209.233.155
